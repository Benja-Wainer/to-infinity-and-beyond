</div>>
<div class="container">
    <div class="card-product my-2">
      <img src="assets/images/default.jpg" />
      <div class="card-product-infos">
        <h2><%= @jetpack.title %></h2>
        <p><%= @jetpack.model %></p>
        <p><strong>$<%= @jetpack.price %>/day</strong></p>
        <p><%= @jetpack.description %></p>
      </div>
    </div>

  <h2>Book Now!</h2>
  <%= simple_form_for [@jetpack, @booking] do |f| %>
    <%= f.input :comment %>
    <%= f.input :booking_date %>
    <%= f.submit "Book this jetpack", class: "btn btn-primary" %>
  <% end %>

    <%= link_to "Return to main list", jetpacks_path %>
</div>
