<%= simple_form_for(jetpack) do |f| %>
  <%= f.input :title %>
  <%= f.input :model %>
  <%= f.input :price %>
  <%= f.input :description %>
  <%= f.submit "Add you jetpack", class: "btn btn-primary" %>
<% end %>
