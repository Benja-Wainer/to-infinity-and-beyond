<div class="container">
  <div class="d-flex justify-content-center">
    <h1><%= current_user.username.capitalize %>, here are your bookings</h1>
  </div>
  <% @bookings.each do |booking| %>
    <div class="card-product my-4">
      <% if booking.jetpack.photo.attached? %>
        <%= cl_image_tag booking.jetpack.photo.key, height: 120, crop: :fill, class: "card-img-top" %>
      <% else %>
        <img class="card-img-top" src=<%= image_path('default.jpg') %> alt="Default jetpack picture">
      <% end %>
      <div class="card-product-infos w-100">
        <div class="row me-4 my-4">
          <p class="col-5"><%= booking.jetpack.title %></p>
          <p class="col-5"><%= booking.comment %></p>
          <%= link_to "Jetpack details", jetpack_path(booking.jetpack), class: "btn btn-warning col-2" %>
        </div>
        <div class="row me-4 my-4">
          <p class="col-5"><%= booking.jetpack.model %></p>
          <p class="col-5"><%= booking.booking_date %></p>
          <%= link_to "Cancel", jetpack_path(booking.jetpack), class: "btn btn-danger col-2" %>
        </div>
      </div>
    </div>
  <% end %>
</div>
